package com.example.data.viewmodel

import androidx.lifecycle.ViewModel

/**
 * When a user clicks the forgot password button,
 * they will be issued a temporary password.
 * This will be done after verifying the email
 * they provide actually has belongs to an
 * already existing account.
 */
class TemporaryPasswordViewModel: ViewModel() {
}